name: Production Deployment
on:
  push:
    branches: [main, dev]
jobs:

  converge:
    name: Converge
    runs-on: self-hosted
    steps:

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
